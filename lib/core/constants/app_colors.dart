import 'package:flutter/material.dart';

class AppColors {
  // Premium Navy & Teal Theme (Unified branding)
  static const Color primary = Color(0xFF1B2430); // Navy Dark
  static const Color secondary = Color(0xFF007A8A); // Teal Accent
  static const Color accent = Color(0xFF007A8A); // Using Teal as accent
  static const Color background = Color(0xFFF0F2F5); // Premium light gray background
  static const Color text = Color(0xFF1B2430); // Dark Navy Text
  static const Color textLight = Color(0xFF6B7280); // Light Grey Text
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);
  static const Color white70 = Color(0xB3FFFFFF); // 70% opacity white
  static const Color white60 = Color(0x99FFFFFF); // 60% opacity white
  static const Color success = Color(0xFF4CAF50); // Green for success
  static const Color error = Color(0xFFFF5252); // Red for error (critical only)
  static const Color warning = Color(0xFFFFC107); // Gold for warning
  static const Color border = Color(0xFFE5E7EB); // Soft Border
  static const Color surface = Color(0xFFFFFFFF); // White surface
  static const Color shadow = Color(0x0F000000); // Very subtle shadow

  // Specific UI components
  static const Color teamHeader = Color(0xFF1B2430);
  static const Color darkRectangle = Color(0xFFF9FAFB); 
  static const Color btnTextColor = Color(0xFFFFFFFF);
  static const Color blue = Color(0xFF007A8A);
  static const Color deepBlue = Color(0xFF1B2430);
  static const Color selectedIcon = Color(0xFF007A8A);
  static const Color unselectedIcon = Color(0xFF9CA3AF);
  
  // GORGEOUS GRADIENTS
  static const List<Color> primaryGradient = [Color(0xFF1B2430), Color(0xFF2D3B4D)];
  static const List<Color> secondaryGradient = [Color(0xFF007A8A), Color(0xFF009688)];
  static const List<Color> cardGradient = [Color(0xFFFFFFFF), Color(0xFFF9FAFB)];
}
